mysqlSecret:
  name: mysql-secret 

deploy:
  name: app-deploy
  dockerSecretName: dockerhub-credentials
  containerImage: thorak2001/spring-petclinic
  containerTag: latest
  mysqlUrl: local

service:
  name: app-service
  type: NodePort
